import React, { Component } from 'react';
import { FM } from 'locales';
import styles from './style.less'

const VideoBtn = () => (
  <div className={styles.VideoBtn}>
    <FM id="aiopen.header.button1"></FM>
  </div>
)

export default VideoBtn;
