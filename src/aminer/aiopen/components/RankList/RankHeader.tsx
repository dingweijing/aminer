import React, { Component } from 'react';
import { FM } from 'locales';
import styles from './style.less'

interface IPropType{}

const RankHeader: React.FC<IPropType> = () => (
  <div className={styles.wrapper} />
)

export default RankHeader;
