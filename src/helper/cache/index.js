import { localCache, LocalStorageCache } from './localStorageCache';
import { DataCache } from './memoryCache';

export default {
  localCache,
  LocalStorageCache,
  DataCache,
};

export {
  localCache, LocalStorageCache,
  DataCache,
};
